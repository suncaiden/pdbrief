# The built site. Rebuilt from content/ on every push by GitHub Actions,
# so there is no reason to commit it.
_site/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# Editors
.vscode/
.idea/

# Issues deleted from the writing desk, kept so they can be restored
.trash/
